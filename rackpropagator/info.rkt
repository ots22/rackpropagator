#lang info

(define collection 'multi)

(define deps '("rackpropagator-lib" "rackpropagator-doc"))
(define implies '("rackpropagator-lib" "rackpropagator-doc"))

(define pkg-desc "Reverse-mode automatic differentiation")

(define pkg-authors '(ots22))
