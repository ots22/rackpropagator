#lang info

(define collection 'multi)

(define deps '("base"
               "rackpropagator-lib"
               "rackunit-lib"))

(define build-deps '())

(define pkg-desc "Rackpropagator tests")

(define pkg-authors '(ots22))
